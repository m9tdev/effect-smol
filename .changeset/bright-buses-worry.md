---
"effect": patch
---

eventlog unencrypted: add StoreMapping service implementations (memory + backing persistence) and tests for resolution, reassignment, and persistence across service recreation.
