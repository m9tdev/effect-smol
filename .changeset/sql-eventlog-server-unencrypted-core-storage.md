---
"effect": patch
---

Implement the core SQL storage semantics for SqlEventLogServerUnencrypted, including per-store sequencing, transactional writes, strict ordered reads, and backlog-safe live change streaming.
