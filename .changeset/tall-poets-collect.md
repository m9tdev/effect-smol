---
"effect": patch
---

Add resolver-based and static store-mapping helpers to EventLogServerUnencrypted and expand focused tests for dynamic resolution, resolver failures, and static shared-store hasStore behavior.
