---
"effect": patch
---

Persist trust-on-first-auth publicKey -> signingPublicKey bindings through each EventLog server Storage service, enforce strict Authenticate mismatch rejection, and reject conflicting concurrent first-bind attempts with Forbidden.
