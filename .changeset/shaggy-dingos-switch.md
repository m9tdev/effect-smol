---
"effect": patch
---

Add read-time historical compaction to `EventLogServerUnencrypted.requestChanges` with `olderThan` support, monotonic representative remote sequences, and shared-StoreId cursor compatibility tests.
