---
"effect": patch
---

eventlog (unstable): run EventLogServerUnencrypted writes with process-before-persist inside store transactions, defer Reactivity invalidation until after commit, and add transactional rollback / commit-visibility / multi-replica concurrency tests.
